var _ = require('underscore'),
    la = require('sylvester'),
    Matrix = la.Matrix,
    Vector = la.Vector,
    numeric = require('numeric');

module.exports = svd;

function svd(m){

    m = Matrix.create(m);
}
