<div class="welcome-screen__detail__progress">

    <div id="demo-thumbnail__details__images" class="welcome-screen__detail__progress__entry">
        IMAGES {{finishedImages.length}}/{{images.length}}
    </div>

    <div id="demo-thumbnail__details__sift" class="welcome-screen__detail__progress__entry">
        FEATURES {{finishedSIFT.length}}/{{images.length}}
    </div>

    <div id="demo-thumbnail__details__bundler" class="welcome-screen__detail__progress__entry">
        BUNDLER
        {{#if bundlerFinished}}
            finished
        {{else}}
            {{#if hasBundler}}
                avaliable
            {{else}}
                not avaliable
            {{/if}}
        {{/if}}
    </div>

    <div id="demo-thumbnail__details__mvs" class="welcome-screen__detail__progress__entry">
        MVS
        {{#if mvsFinished}}
            finished
        {{else}}
            {{#if hasMVS}}
                avaliable
            {{else}}
                not avaliable
            {{/if}}
        {{/if}}
    </div>

</div>