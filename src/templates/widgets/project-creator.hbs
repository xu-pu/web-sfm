<div class="project-creator__input">

    {{view view.InputView}}

    {{#unless view.isInvalid}}
        <div class="project-creator__submit" {{action "createProject" target=view}}>
            <h1>Create</h1>
        </div>
    {{/unless}}

</div>